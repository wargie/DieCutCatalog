<Window x:Class="DieCutCatalog.Desktop.Views.PasswordConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение операции" Width="440" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="#F3F5F7" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" FontSize="20" FontWeight="SemiBold" Foreground="#18272D" />
        <TextBlock x:Name="MessageText" Grid.Row="1" Margin="0,8,0,18" TextWrapping="Wrap"
                   Foreground="#5F6D73" />
        <StackPanel Grid.Row="2">
            <TextBlock Text="Пароль текущего пользователя" Margin="0,0,0,5"
                       FontSize="12" FontWeight="SemiBold" Foreground="#4B5960" />
            <PasswordBox x:Name="PasswordInput" MinHeight="36" Padding="10,7"
                         KeyDown="PasswordInput_KeyDown" />
            <TextBlock x:Name="ValidationText" Margin="0,6,0,0" Foreground="#B42318"
                       Visibility="Collapsed" Text="Введите пароль." />
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,22,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" MinWidth="92" MinHeight="36" Margin="0,0,10,0"
                    Padding="16,6" Click="Cancel_Click" />
            <Button x:Name="ConfirmButton" Content="Подтвердить" MinWidth="120" MinHeight="36"
                    Padding="16,6" Background="#147D73" Foreground="White" BorderThickness="0"
                    FontWeight="SemiBold" Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>