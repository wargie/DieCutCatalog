<Window x:Class="DieCutCatalog.Desktop.Views.PasswordConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Danger zone" Width="460" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        WindowStyle="None" Background="#FFFFFF" ShowInTaskbar="False">
    <Border BorderBrush="#8F1D18" BorderThickness="1" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Background="#B42318" CornerRadius="5,5,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="18,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Danger zone" VerticalAlignment="Center"
                               FontSize="16" FontWeight="SemiBold" Foreground="White" />
                    <Button Grid.Column="1" Width="34" Height="34" Content="×"
                            FontSize="20" Foreground="White" Background="Transparent"
                            BorderThickness="0" Cursor="Hand" Click="Close_Click" />
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="24,20,24,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="TitleText" FontSize="19" FontWeight="SemiBold" Foreground="#7A1C18" />
                <TextBlock x:Name="MessageText" Grid.Row="1" Margin="0,8,0,18" TextWrapping="Wrap"
                           Foreground="#5F4A49" />
                <StackPanel Grid.Row="2">
                    <TextBlock Text="Пароль суперпользователя" Margin="0,0,0,5"
                               FontSize="12" FontWeight="SemiBold" Foreground="#4B3534" />
                    <PasswordBox x:Name="PasswordInput" MinHeight="36" Padding="10,7"
                                 BorderBrush="#C9A4A1" KeyDown="PasswordInput_KeyDown" />
                    <TextBlock x:Name="ValidationText" Margin="0,6,0,0" Foreground="#B42318"
                               Visibility="Collapsed" Text="Введите пароль суперпользователя." />
                </StackPanel>
                <StackPanel Grid.Row="3" Margin="0,22,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Отмена" MinWidth="92" MinHeight="36" Margin="0,0,10,0"
                            Padding="16,6" Click="Cancel_Click" />
                    <Button x:Name="ConfirmButton" Content="Подтвердить" MinWidth="120" MinHeight="36"
                            Padding="16,6" Background="#B42318" Foreground="White" BorderThickness="0"
                            FontWeight="SemiBold" Click="Confirm_Click" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>